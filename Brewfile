# Brewfile — managed by install.sh via `brew bundle`.
# Run `brew bundle --file=Brewfile` to install everything, or
# `brew bundle cleanup --file=Brewfile` to see what's installed but not listed.

# CLI tools
brew "fzf"               # fuzzy finder
brew "fd"                # fast find
brew "ripgrep"           # fast grep (rg)
brew "neovim"            # editor (nvim)
brew "tmux"              # terminal multiplexer
brew "eza"               # modern ls
brew "git-delta"         # better git diffs
brew "lnav"              # log file navigator
brew "tree-sitter-cli"   # tree-sitter parser tooling
brew "jq"                # JSON processor
brew "gh"                # GitHub CLI
brew "shellcheck"        # shell script linter

# Terminals
cask "cmux"              # AI-agent terminal with vertical tabs
brew "herdr"             # agent multiplexer (herdr.dev)
# Alacritty is NOT listed here on purpose — its cask fails Homebrew's Gatekeeper
# check and is disabled on 2026-09-01. install.sh pulls the release DMG instead.

# Fonts (homebrew/cask-fonts tap was deprecated — fonts now live in the main cask repo)
cask "font-roboto-mono-nerd-font"
